import styled from 'styled-components';

export const LocalComponents = {
  Container: styled.div`
    width: 100vw;
    height: fit-content;

    padding: 32px;

    display: flex;
    gap: 16px;
  `,
};
